{
  "name": "omni-extension-sharp",
  "packageManager": "yarn@3.3.1",
  "scripts": {
    "build": "esbuild --bundle ./extension.ts --outdir=../server  --format=esm --analyze --color=true --target=es2020 --platform=node --external:sharp --external:omni-sockets"
  },
  "dependencies": {
    "sharp": "^0.32.6"
  },
  "devDependencies": {
    "esbuild": "^0.19.3",
    "typescript": "^5.2.2"
  }
}
